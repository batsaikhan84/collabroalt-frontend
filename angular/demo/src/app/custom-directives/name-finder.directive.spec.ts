import { NameFinderDirective } from './name-finder.directive';

describe('NameFinderDirective', () => {
  it('should create an instance', () => {
    const directive = new NameFinderDirective();
    expect(directive).toBeTruthy();
  });
});
