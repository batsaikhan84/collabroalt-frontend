import { NameSplitPipe } from './name-split.pipe';

describe('NameSplitPipe', () => {
  it('create an instance', () => {
    const pipe = new NameSplitPipe();
    expect(pipe).toBeTruthy();
  });
});
